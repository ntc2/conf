# The ~/.zprofile is only loaded for login shells ... but I'm not sure
# which shells those are. In particular, when logging in through a
# graphical login manager like GDM, *no* login shells are started and
# so this file is never sourced.
