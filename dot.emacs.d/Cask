;; -*- emacs-lisp -*-

;; This file was originally generated by `cd ~/.emacs.d; cask init`.

(source gnu)
;; This is unstable Melpa; there is also a `melpa-stable` source.
(source melpa)

;; Defaults from `cask init`.

(depends-on "bind-key")
(depends-on "cask")
(depends-on "dash")
(depends-on "drag-stuff")
(depends-on "exec-path-from-shell")
(depends-on "expand-region")
(depends-on "f")
(depends-on "flycheck")
(depends-on "flycheck-cask")
(depends-on "htmlize")
(depends-on "idle-highlight-mode")
(depends-on "magit")
(depends-on "multiple-cursors")
(depends-on "nyan-mode")
(depends-on "pallet")
(depends-on "popwin")
(depends-on "prodigy")
(depends-on "projectile")
(depends-on "s")
(depends-on "smartparens")
(depends-on "smex")
(depends-on "use-package")
(depends-on "web-mode")
(depends-on "yasnippet")

;; Packages I depend on. Some are duplicated above.

(depends-on "haskell-mode")
(depends-on "flycheck")
;; Chris Done's version of `flycheck-haskell`.
;;
;; Install from GitHub, since there is no Emacs package proper.
;;
;; This causes problems on fresh installs with no GitHub SSH keys.

;;(depends-on "haskell-flycheck"
;;  :git "git@github.com:chrisdone/haskell-flycheck.git"
;;  :files ("*.el"))

;; Another Haskell IDE mode.
(depends-on "intero")
;; Install development version of Intero. This works, but is a little
;; confusing because it ends up in
;; ~/.emacs.d/.cask/25.3/elpa/intero-<date>.<time>, the same as if I
;; install from MELPA.
;;
;; (depends-on "intero"
;;             :git "https://github.com/commercialhaskell/intero.git"
;;             :ref "7db6ba44572dc8207a2c6d24c14e06206f7426d2"
;;             :files ("elisp/intero.el"))

;; Show Git status of lines in "gutter" (left margin).
;;
;;(depends-on "git-gutter+")
;;(depends-on "git-gutter-fringe+") ;; Version that works together with linum-mode, but not in ttys.
;;
;;(depends-on "git-gutter")
;;
;; Using git-gutter-fringe because it plays well with linum-mode.
(depends-on "fringe-helper")     ;; Dep of git-gutter-fringe.
(depends-on "git-gutter-fringe") ;; Works with linum-mode. same namespace as git-gutter. delete other before using

;; Markdown.
;;
;; More recent than the version that ships with Emacs.
(depends-on "markdown-mode")
;; For syntax highlighting in "GFM fenced code blocks", e.g.
;;
;; ``` haskell
;; module Test where
;; ```
(depends-on "polymode")
(depends-on "poly-markdown")

;; Use-package.
;;
;; This can replace Cask: i.e. it can auto install things.
;;
;; https://github.com/jwiegley/use-package
;;
;; At work straight.el has been suggested as a Cask replacement.
(depends-on "use-package")
(depends-on "diminish")

;; Helm.
;;
;; https://github.com/emacs-helm/helm
(depends-on "helm")
(depends-on "helm-descbinds")
(depends-on "projectile")
(depends-on "helm-projectile")
(depends-on "helm-ag")

;; Trim whitespace at end of edited lines.
(depends-on "ws-butler")

;; Edit Firefox/Chrome text areas in Emacs.
(depends-on "atomic-chrome")

;; Move buffers with ease.
(depends-on "buffer-move")

;; Typescript (used in Haddock to generate JS)
(depends-on "typescript-mode")

;; To get `debian-changelog-mode'.
(depends-on "dpkg-dev-el")
