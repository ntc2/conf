# -*- conf -*-
#
# This file being '~/.gitignore' is non-standard and configured by
# 'core.excludesFile' in '~/.gitconfig'.
#
# Note that there is also '<repo>/.git/info/exclude' for repo local,
# non-versioned ignores.

# Emacs
.emacs.desktop
.emacs.desktop.lock
TAGS
.projectile
.dir-locals.el

# Python
*.pyc

# Java
*.class

# Haskell
*.o
*.hi
.stack-work/
