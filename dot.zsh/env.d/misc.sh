# Miscellaneous things that don't warrant their own category. Could
# put these in the top-level dot.zshenv instead.

# Open a file using the "right" program automatically. Note that this
# even with urls: it opens them in your browser.
alias open=xdg-open
